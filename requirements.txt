# ─────────────────────────────────────────────────────────────────────────────
# MechArm 270 Assembly Vision Dashboard — Python Dependencies
# Install with:  pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# Web server & real-time WebSocket transport
flask>=2.3.0
flask-socketio>=5.3.0

# WSGI server used by Flask-SocketIO in production mode
# (werkzeug is installed automatically with flask, listed for clarity)
werkzeug>=2.3.0

# HTTP client — used for Rail ESP32 HTTP API calls
requests>=2.31.0

# MechArm 270 robot arm SDK (Wi-Fi TCP socket control)
pymycobot>=3.5.0

# Computer vision — frame capture, black-pixel ROI masking, annotation, MJPEG stream
opencv-python>=4.8.0

# Numerical array operations required by OpenCV pipelines
numpy>=1.24.0
